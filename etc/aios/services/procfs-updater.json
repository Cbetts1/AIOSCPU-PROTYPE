{
  "name": "procfs-updater",
  "description": "ProcFS Updater — refreshes /proc tree with live process and memory data every 5 seconds",
  "target": "multi-user",
  "command": "procfs-updater",
  "restartPolicy": "on-failure",
  "maxRestarts": 5,
  "restartDelay": 3000,
  "interval": 0,
  "enabled": true,
  "dependsOn": [],
  "env": {}
}
