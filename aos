#!/usr/bin/env node
'use strict';
/**
 * aos — AIOSCPU OS Integration Layer entry point
 *
 * Usage (Termux / Linux / macOS):
 *   chmod +x aos
 *   ./aos
 *
 * Or:
 *   node aos
 *   npm start
 *
 * Requires: Node.js >= 14.  No external npm packages needed.
 *
 * Boot flow:
 *   bootstrap.js → pivot.js → init.js → services → terminal
 */
require('./boot/bootstrap.js').start();
